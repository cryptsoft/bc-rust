[package]
name = "bouncycastle-core-interface"
version = "0.1.1"
edition = "2018"

[workspace]

[features]
default = ["std"]
std = ["bouncycastle-utils/std"]

[dependencies]
bouncycastle-utils = { path = "../utils", version = "0.1.1", default-features = false }